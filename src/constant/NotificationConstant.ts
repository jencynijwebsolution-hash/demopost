export const USER_ROLE_CONSTANT = Object.freeze({
    ADMIN: "Admin",
    USER: "User"
})

export const NOTIFICATION_TYPE_CONSTANT = Object.freeze({
    PROFILE_VERIFICATION: "verify_otp",
    USER_STATUS: "status"
})

export const NOTIFICATION_MODEL_CONSTANT = Object.freeze({
    USER: "user"
})

export const NOTIFICATION_ACCOUNT_STATUS = Object.freeze({
    PENDING: "user account pending",
    ACTIVE: "user account active",
    DEACTIVE: "user account deactive"
})
